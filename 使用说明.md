# LaTeX 格式面板使用说明

## 1. 安装

1. 下载 Release 中的 `LaTeX格式面板-便携安装包-v2.0.2.zip` 并解压。
2. 双击 `双击安装.cmd`。
3. 重启 VS Code，在左侧活动栏打开“LaTeX 格式面板”。

如果双击安装没有成功，也可以在 VS Code 的“扩展”页面点击右上角“…”→“从 VSIX 安装”，选择 `latex-format-palette.vsix`。

## 2. 基本用法

打开 `.tex` 文件，把光标放在要插入的位置，再点击面板中的格式卡片。没有选中文字时，插件会插入可直接填写的占位内容；选中文字后，部分格式会自动包裹选区。

面板顶部的“新建格式”可以创建自己的段落、标题、表格、图片和公式样式。样式会保存在 VS Code 的扩展数据中，也可以通过“导出”生成 `.latexstyles` 文件分享给朋友。

## 3. 段落和标题

段落样式可以选择中文字号、行距、段前段后距离、左右缩进、首行缩进或悬挂缩进，以及左对齐、居中、右对齐和两端对齐。行距和间距均可选择常用值或“自定义”。

标题样式可以设置级别、字号、粗体、对齐方式以及标题前后的距离。

## 4. 表格

在“新建格式”中选择“表格样式”，先设置一次：

- 表格内容的字号、字体、行距和对齐方式；
- 表格名称（表格注释）的文字、字号、粗体、斜体、位置和对齐方式；
- 表格位置、边框和三线表选项。

写作时点击表格样式，会打开一个类似 Word 的网格。拖动或点击选择需要的行数和列数，再按提示填写“第 1 行第 1 列”等单元格内容。插入后把光标放进表格代码，右键可重新编辑表格属性。

## 5. 图片

在“新建格式”中选择“图片样式”，预设图片大小（原图、百分比或自定义像素）、位置、图注字号、对齐方式和编号规则。

点击图片样式后会打开图片选择窗口。可以把图片拖入窗口，也可以点击“打开文件资源管理器”选择图片。随后填写图片说明和引用名称，确认后插入。

图片插入后，把光标放在 figure 代码中并右键选择“修改图片属性”，即可在预览后调整缩放、位置、浮动方式、旋转、裁剪、图注样式和自定义编号（例如“1-1”）。

## 6. 公式

点击“公式”打开独立公式工具。选择普通公式、分数、上标、下标、根号、求和、积分、矩阵或分段函数，再点击蓝色输入槽填写内容。按 Tab 可移动到下一个输入槽，右侧会实时显示预览。

公式可以选择行内、独立、自动编号或手动编号，也可以保存到“我的公式”和“变量库”。把光标放入已插入的公式代码中，右键可以再次打开并修改。

## 7. 论文、目录和页面

- “论文方案”：一次设置正文、标题、图片和表格的协调格式。
- “文档工具”：一键新建带标题、作者、日期、摘要和目录的 `.tex` 文件。
- “目录”：插入或更新带页码的目录。
- “页面设置”：设置页眉、页脚、页码、首页规则和页边距。
- “导航引用”：在章节、图片和表格之间快速跳转或插入引用。
- “PDF 预览”：调用 LaTeX Workshop 查看编译结果。

插件不会检测或修改 MiKTeX、TeX Live 或 LaTeX Workshop 的安装配置。编译问题仍需根据 LaTeX Workshop 的日志处理。
